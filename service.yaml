apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  type: NodePort
  # Uncomment the below line to create a Headless Service
  # clusterIP: None
  selector:
    app: example
    department: cicd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 7080
